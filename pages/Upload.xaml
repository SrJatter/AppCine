<Page x:Class="SideBar_Nav.Pages.Upload"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Upload">
    <Grid Name="MainGrid" Background="{DynamicResource Backdrop_Page}">
		<TextBlock Foreground="{DynamicResource Text}" Text = "Sistema de Gestión de Cine" FontSize = "50" HorizontalAlignment = "Center" VerticalAlignment = "Top" Margin = "0, 10, 0, 0"/>
		<!-- Pantalla inicial con dos botones -->
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Content="Subir Archivo.txt" Width="200" Height="50" Margin="10" Click="ShowFileUploadScreen"/>
            <Button Content="Subir mediante Widgets" Width="200" Height="50" Margin="10" Click="ShowWidgetUploadScreen"/>
        </StackPanel>
    </Grid>
</Page>
